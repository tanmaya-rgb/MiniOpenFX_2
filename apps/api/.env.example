PORT=3000

DATABASE_URL=postgres://miniopenfx:miniopenfx@localhost:5432/miniopenfx

REDIS_URL=redis://localhost:6379

# Bearer token for the single seeded API client (see src/db/seed.ts)
SEEDED_API_KEY=dev-local-api-key

# TTL for cached indicative prices, in milliseconds
PRICE_CACHE_TTL_MS=1500
